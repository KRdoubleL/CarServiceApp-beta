import React from 'react'
import styled from 'styled-components'

const DashboardContent = ({classname}) => {
    return (
        <div className={classname}>

        </div>
    )
}

const Dashboard = styled(DashboardContent)`
    
`

export default Dashboard
